# Customer Churn Prediction

This repository implements a Customer Churn Prediction System using an existing dataset (recommended: Telco Customer Churn).

## Week 1 (current)
- Load and clean dataset
- Exploratory Data Analysis (EDA)
- Save plots to `reports/figures/`
- Auto-generate a Week 1 report: `reports/week1_findings.md`

## Setup
```bash
python -m venv .venv
source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
pip install -r requirements.txt
